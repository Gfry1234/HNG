import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Gfry.utilityapp',
  appName: 'Smart Utility Toolkit',
  webDir: 'out'
};

export default config;
